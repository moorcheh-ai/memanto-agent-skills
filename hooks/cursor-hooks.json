{
  "hooks": {
    "sessionStart": [
      {
        "command": "python \"${PLUGIN_ROOT}/hooks/session_start.py\" --host cursor"
      }
    ],
    "preCompact": [
      {
        "command": "python \"${PLUGIN_ROOT}/hooks/session_start.py\" --host cursor"
      }
    ]
  }
}
